/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.startech.com.bd",
      "images.unsplash.com",
      "via.placeholder.com",
      "example.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;